name: blank

on:
  push:

jobs:
  classical:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        mainland: ["feasible", "identity", "surrender", "earn", "clause"]
        patiently: ["reward", "harmful", "simply", "cabin"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar dZ
